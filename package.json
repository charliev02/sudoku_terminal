{
  "scripts": {
    "start": "npx tsc -w",
    "lint": "gts lint",
    "clean": "gts clean",
    "compile": "tsc",
    "fix": "gts fix",
    "prepare": "npm run compile",
    "pretest": "npm run compile",
    "posttest": "npm run lint"
  },
  "dependencies": {
    "typescript": "^5.3.3"
  },
  "devDependencies": {
    "@types/node": "20.8.2",
    "gts": "^5.2.0",
    "nodemon": "^3.0.3",
    "ts-node": "^10.9.2",
    "typescript": "~5.2.0"
  }
}
